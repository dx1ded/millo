mixin select(current, list)
  .select
    span.text.text--xs.select__display= list[current]
    ul.list-reset.select-list
      - list.splice(current, 1)
      each item in list
        li.text.text--xs.select-list__item= item
